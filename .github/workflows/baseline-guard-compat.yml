name: Baseline Guard
on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verifyExpected:
    # The job name MUST be exactly "verifyExpected" to produce the required context:
    # "Baseline Guard / verifyExpected"
    name: verifyExpected
    runs-on: ubuntu-latest
    if: always()
    steps:
      - run: echo "Compat status to satisfy branch protection (no PR code executed)."
