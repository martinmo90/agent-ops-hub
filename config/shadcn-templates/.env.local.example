# Copy this to apps/shadcn-chatbot-kit/apps/www/.env.local and fill in your keys
# Do not commit .env.local - it's gitignored

ANTHROPIC_API_KEY=your_anthropic_key_here

# Optional: if the kit supports choosing a provider/model via env
# AI_PROVIDER=anthropic
# ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Note: Default port is 3333 (configured in package.json dev script)
NEXT_PUBLIC_APP_URL=http://localhost:3333
