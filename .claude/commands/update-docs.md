# /update-docs
Goal: Minimal, accurate doc updates tied to changes.

Steps:
- Identify user-visible behavior/CLI/config updates.
- Edit relevant files under `docs/` and/or README.
- Add a one-line entry to `CHANGELOG.md` (Unreleased).

Evidence: list changed files + sha256.
